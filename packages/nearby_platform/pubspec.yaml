name: nearby_platform
description: Protected storage and mDNS platform adapters for MeowWatch Nearby.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0
  flutter: ">=3.44.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: ^11.1.1
  nearby_bridge:
    path: ../nearby_bridge
  nsd: ^5.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^6.0.0
